const fs = require('fs'); const lines = fs.readFileSync('e:/Projects/HireHuub-HRMS/apps/web/src/pages/Finance/transactions/TransactionsPage.tsx', 'utf-8').split('\n'); lines.forEach((line, i) => { if (line.includes('Expense Type *') || line.includes('handleRecordExpense') || line.includes('filteredExpenses =') || line.toUpperCase().includes('TOTAL EXPENSE') || line.includes('const [expenseTypesList')) { console.log(i + ': ' + line.trim()); } });
