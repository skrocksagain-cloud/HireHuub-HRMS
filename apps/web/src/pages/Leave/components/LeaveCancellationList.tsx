import type { LeaveRequest } from '../types/leave';

export const LeaveCancellationList = ({ requests, disabled, onCancel }: { requests: LeaveRequest[]; disabled: boolean; onCancel: (requestId: string) => void; }) => { const pending = requests.filter(({ status }) => status === 'Pending'); if (pending.length === 0) return null; return <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="text-base font-semibold text-slate-900">Pending requests</h2><div className="mt-3 space-y-2">{pending.map((request) => <div key={request.id} className="flex items-center justify-between gap-3 rounded-lg border border-slate-200 p-3 text-sm"><span>{request.leaveType} · {request.startDate} to {request.endDate}</span><button type="button" onClick={() => onCancel(request.id)} disabled={disabled} className="rounded-lg border border-rose-300 px-3 py-1 text-rose-700 disabled:opacity-50">Cancel</button></div>)}</div></section>; };
