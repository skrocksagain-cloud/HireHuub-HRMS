import { useState } from 'react';

import type { LeaveBalance } from '../types/leave';

interface Props { balances: LeaveBalance[]; canManage: boolean; disabled: boolean; onCarryForward: (balanceId: string, days: number) => void; }
export const LeaveBalances = ({ balances, canManage, disabled, onCarryForward }: Props) => { const [days, setDays] = useState<Record<string, string>>({}); return <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="text-base font-semibold text-slate-900">Leave balance</h2>{balances.length === 0 ? <p className="mt-4 text-sm text-slate-500">No leave balances are available.</p> : <div className="mt-4 space-y-2">{balances.map((balance) => <div key={balance.id} className="flex flex-wrap items-center justify-between gap-3 rounded-lg border border-slate-200 p-3 text-sm"><span><strong>{balance.leaveType}</strong> · Available: {balance.available} · Used: {balance.used} · Carried: {balance.carriedForward}</span>{canManage && <div className="flex gap-2"><input aria-label={`${balance.leaveType} carry forward days`} value={days[balance.id] ?? ''} onChange={(event) => setDays({ ...days, [balance.id]: event.target.value })} placeholder="Carry days" inputMode="numeric" className="w-28 rounded-lg border border-slate-300 px-2 py-1" disabled={disabled} /><button type="button" onClick={() => onCarryForward(balance.id, Number(days[balance.id]))} disabled={disabled} className="rounded-lg bg-slate-700 px-3 py-1 text-white disabled:opacity-50">Carry forward</button></div>}</div>)}</div>}</section>; };
