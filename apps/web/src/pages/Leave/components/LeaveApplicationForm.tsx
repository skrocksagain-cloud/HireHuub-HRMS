import { useState, type FormEvent } from 'react';

import type { LeaveApplicationInput } from '../types/leave';

interface Props { disabled: boolean; onSubmit: (input: LeaveApplicationInput) => void; }
export const LeaveApplicationForm = ({ disabled, onSubmit }: Props) => { const [input, setInput] = useState<LeaveApplicationInput>({ requestType: 'Leave', leaveType: '', startDate: '', endDate: '', reason: '', medicalCertificateReference: '' }); const submit = (event: FormEvent<HTMLFormElement>): void => { event.preventDefault(); onSubmit(input); setInput({ ...input, reason: '', medicalCertificateReference: '' }); }; const update = (changes: Partial<LeaveApplicationInput>): void => setInput({ ...input, ...changes }); return <form onSubmit={submit} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="text-base font-semibold text-slate-900">Apply for leave</h2><div className="mt-4 grid gap-3 sm:grid-cols-2"><select aria-label="Request type" value={input.requestType} onChange={(event) => update({ requestType: event.target.value as LeaveApplicationInput['requestType'] })} className="rounded-lg border border-slate-300 px-3 py-2" disabled={disabled}><option>Leave</option><option>Comp Off</option></select><input aria-label="Leave type" value={input.leaveType} onChange={(event) => update({ leaveType: event.target.value })} placeholder="Leave type" className="rounded-lg border border-slate-300 px-3 py-2" disabled={disabled} /><input aria-label="Start date" type="date" value={input.startDate} onChange={(event) => update({ startDate: event.target.value })} className="rounded-lg border border-slate-300 px-3 py-2" disabled={disabled} /><input aria-label="End date" type="date" value={input.endDate} onChange={(event) => update({ endDate: event.target.value })} className="rounded-lg border border-slate-300 px-3 py-2" disabled={disabled} /></div><input aria-label="Medical certificate reference" value={input.medicalCertificateReference} onChange={(event) => update({ medicalCertificateReference: event.target.value })} placeholder="Medical certificate reference (if applicable)" className="mt-3 w-full rounded-lg border border-slate-300 px-3 py-2" disabled={disabled} /><textarea aria-label="Leave reason" value={input.reason} onChange={(event) => update({ reason: event.target.value })} placeholder="Reason" className="mt-3 min-h-24 w-full rounded-lg border border-slate-300 px-3 py-2" disabled={disabled} /><button type="submit" disabled={disabled} className="mt-3 rounded-lg bg-green-700 px-4 py-2 text-sm font-medium text-white disabled:opacity-50">Submit request</button></form>; };
