import { LEAVE_STATUS_STYLES } from '../constants/leave';
import type { LeaveRequest } from '../types/leave';
export const LeaveCalendar = ({ requests }: { requests: LeaveRequest[] }) => <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="text-base font-semibold text-slate-900">Leave calendar</h2>{requests.length === 0 ? <p className="mt-4 text-sm text-slate-500">No leave requests to display.</p> : <div className="mt-4 space-y-2">{requests.map((request) => <div key={request.id} className="flex flex-wrap items-center justify-between gap-2 rounded-lg border border-slate-200 p-3 text-sm"><span><strong>{request.employeeName}</strong> · {request.startDate} to {request.endDate}</span><span className={`rounded-full px-2 py-1 text-xs font-semibold ${LEAVE_STATUS_STYLES[request.status]}`}>{request.status}</span></div>)}</div>}</section>;
