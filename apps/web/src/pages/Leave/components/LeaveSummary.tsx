import type { LeaveDashboardData } from '../types/leave';
import { getLeaveSummary } from '../utils/leave';
export const LeaveSummary = ({ data }: { data: LeaveDashboardData }) => { const summary = getLeaveSummary(data.requests, data.balances.reduce((total, balance) => total + balance.carriedForward, 0)); const cards = [['Available balance', String(data.balances.reduce((total, balance) => total + balance.available, 0))], ['Approved days', String(summary.approvedDays)], ['Pending days', String(summary.pendingDays)], ['Carry forward', String(summary.carriedForward)]]; return <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(([label, value]) => <div key={label} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><p className="text-sm text-slate-500">{label}</p><p className="mt-2 text-2xl font-semibold text-slate-900">{value}</p></div>)}</div>; };
